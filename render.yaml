services:
  - type: web
    name: history-of-music-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production

  # Legacy fallback if a host still points at backend/servers:
  # startCommand: node servers/index.js
